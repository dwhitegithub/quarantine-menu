@page
@model QuarantineMenu.Pages.Menus.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Menu[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Menu[0].MealID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Menu[0].FoodID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Menu) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MealID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoodID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MenuID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MenuID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MenuID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
